import styled from 'styled-components';

export const TeamsSelection = styled.div`
  display: flex;
  width: 60%;
  margin: 5px 0 5px 0;
  background: none;
  padding: 2px;
  border-radius: 5px;
  border: 1px solid grey;
  cursor: pointer;

  &:hover {
    filter: brightness(130%);
  }
`;
